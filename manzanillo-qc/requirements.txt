# Core quantum
pennylane>=0.36
pennylane-lightning>=0.36

# Neutral-atom / Pulser
pulser>=0.18
pulser-simulation>=0.18

# MPS backend for Pulser/Pasqal at n≥16 (~2 GB install with torch)
emu-mps>=2.6

# D-Wave simulated annealing (CPU, no cloud credentials needed)
dimod>=0.12
dwave-samplers>=1.0

# Classical solvers + numerics
numpy>=1.24
pandas>=2.0
scipy>=1.10

# Geography (land mask + sensor map coastlines)
cartopy>=0.21
shapely>=2.0

# Plotting + data
matplotlib>=3.7
requests>=2.28

# Config
pydantic>=2.0
pyyaml>=6.0

# Pasqal qubo-solver: pulls in torch + torch-geometric, install separately:
#   pip install git+https://github.com/pasqal-io/qubo-solver
